# Optional. If set, Layer 3 agents (Triage, Planning, Implementation) call
# the real Anthropic API for their narrative reasoning steps. If unset, the
# pipeline runs fully offline with deterministic fallback text — useful for
# a demo with no API key or network access.
ANTHROPIC_API_KEY=

PORT=8000
